import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Fit or fill a generic aspect preset around a focal point in a fixed composition.',primaryLabel:'crop CSS',related:['aspect','proportion']});
