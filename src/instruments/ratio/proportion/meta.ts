import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Apply an exact named relationship to a frame and see rounded pixel dimensions.',primaryLabel:'proportioned dimensions',related:['aspect','crop']});
