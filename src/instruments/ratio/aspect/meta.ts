import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Reduce a frame ratio and solve a new pixel size from a desired ratio.',primaryLabel:'aspect-ratio CSS',related:['proportion','crop']});
