import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Make a two-stop sRGB gradient with a real seeded noise overlay.',primaryLabel:'noise gradient CSS',related:['grain','texture']});
