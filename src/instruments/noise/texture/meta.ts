import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Make a seeded procedural texture and export matching SVG or PNG locally.',primaryLabel:'texture SVG',related:['grain','gradient']});
