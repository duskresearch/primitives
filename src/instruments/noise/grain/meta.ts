import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Apply deterministic SVG filter grain over a chosen background color.',primaryLabel:'grain SVG',related:['gradient','texture']});
