import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Build a linear or modular spacing ramp and copy CSS properties.',primaryLabel:'spacing CSS',related:['inset','tokens']});
