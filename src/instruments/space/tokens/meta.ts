import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Export the shared spacing scale as CSS, JSON, or Tailwind CSS v3 configuration.',primaryLabel:'spacing tokens',related:['scale','inset']});
