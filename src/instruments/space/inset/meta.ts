import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Set four padding and margin sides on a card and copy compact CSS.',primaryLabel:'inset CSS',related:['scale','tokens']});
