import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({ purpose: 'Compare a rounded rectangle with a superellipse-based corner and copy its path.', primaryLabel: 'SVG path', related: ['form', 'polygon', 'blob'] });
