import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({ purpose: 'Choose a geometric form and copy its compact, standalone SVG.', primaryLabel: 'SVG', related: ['polygon', 'blob', 'favicon'] });
