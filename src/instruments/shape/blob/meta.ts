import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({ purpose: 'Set a seed and radial variation, then copy a repeatable organic SVG path.', primaryLabel: 'SVG path', related: ['form', 'polygon', 'corner'] });
