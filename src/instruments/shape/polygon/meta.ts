import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({ purpose: 'Edit ordered polygon vertices and copy the CSS clip-path they define.', primaryLabel: 'clip-path', related: ['form', 'corner', 'blob'] });
