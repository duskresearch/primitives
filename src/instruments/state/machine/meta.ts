import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Edit and step through a flat deterministic state machine.',primaryLabel:'valid machine JSON',related:['export']});
