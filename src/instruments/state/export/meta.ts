import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Copy XState v5 configuration or a pure JavaScript transition function.',primaryLabel:'machine export',related:['machine']});
