import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Estimate movement time from travel distance and element size.',primaryLabel:'duration',related:['ease','spring','stagger']});
