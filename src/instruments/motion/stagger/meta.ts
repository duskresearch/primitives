import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Order a list entrance and copy each item’s start delay.',primaryLabel:'delay list',related:['duration','ease','export']});
