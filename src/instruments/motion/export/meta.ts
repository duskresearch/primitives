import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Write the selected curve for CSS, Motion for React, or SwiftUI.',primaryLabel:'motion export',related:['ease','spring','duration']});
