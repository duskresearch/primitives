import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Adjust a timing curve and compare its movement with linear motion.',primaryLabel:'CSS timing',related:['spring','duration','export']});
