import { defineInstrument } from '@/lib/instrument';
export default defineInstrument({purpose:'Tune a damped spring and copy a sampled CSS timing function.',primaryLabel:'CSS spring',related:['ease','duration','export']});
