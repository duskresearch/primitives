import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'See what two colors make together: mixed, laid over each other, or blended as CSS does.',
  primaryLabel: 'result',
  related: ['harmony', 'contrast', 'convert'],
});
