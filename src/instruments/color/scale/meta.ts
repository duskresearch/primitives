import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'Seven even steps from one color, lightest to darkest, ready to paste as CSS.',
  primaryLabel: 'CSS',
  related: ['contrast', 'harmony', 'pick'],
});
