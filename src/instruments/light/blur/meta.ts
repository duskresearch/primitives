import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Adjust blur and transparency over a fixed specimen and inspect its modeled contrast.',primaryLabel:'blur CSS',related:['shadow','elevation']});
