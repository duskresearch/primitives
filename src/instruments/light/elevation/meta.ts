import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Generate a consistent ladder of shadow levels from one light model.',primaryLabel:'elevation tokens',related:['shadow','blur']});
