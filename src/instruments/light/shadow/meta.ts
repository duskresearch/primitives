import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Build a layered box shadow from explicit angle, lift, softness and opacity.',primaryLabel:'box-shadow CSS',related:['elevation','blur']});
