import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Reorder a list reproducibly while preserving duplicate and Unicode entries.',primaryLabel:'shuffled list',related:['seed','range','dice']});
