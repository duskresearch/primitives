import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Make a repeatable uint32 sequence and copy its seed or values.',primaryLabel:'seeded sequence',related:['range','dice','shuffle']});
