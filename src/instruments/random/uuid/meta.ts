import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Inspect a deterministic UUID example or generate a replayable v4 or v7 ID.',primaryLabel:'UUID',related:['seed','range','dice']});
