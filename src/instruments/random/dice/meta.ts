import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Roll bounded dice and see each roll, the sum and possible limits.',primaryLabel:'dice total',related:['seed','range','shuffle']});
