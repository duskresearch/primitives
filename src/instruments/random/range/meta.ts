import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Draw a seeded, unbiased integer between inclusive endpoints.',primaryLabel:'random number',related:['seed','dice','shuffle']});
