import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'Set columns, gaps and container padding, then copy the CSS for the grid you see.',
  primaryLabel: 'grid CSS',
  related: ['breakpoints', 'layout', 'baseline'],
});
