import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'Compare text line boxes with a vertical rhythm, then copy a step and compatible line height.',
  primaryLabel: 'baseline step',
  related: ['columns', 'layout', 'type/scale'],
});
