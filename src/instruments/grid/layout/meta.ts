import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'Choose a page skeleton, test its collapse width, and copy the matching CSS and markup.',
  primaryLabel: 'layout CSS',
  related: ['columns', 'breakpoints', 'baseline'],
});
