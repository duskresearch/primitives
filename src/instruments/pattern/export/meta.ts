import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Copy the repeating SVG or download its matching SVG and PNG tile locally.',primaryLabel:'pattern SVG',related:['tile','stripes']});
