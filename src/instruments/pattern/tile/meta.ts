import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Shape one geometric motif and copy a self-contained repeating SVG.',primaryLabel:'pattern SVG',related:['stripes','export']});
