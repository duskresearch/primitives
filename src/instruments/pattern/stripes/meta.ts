import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Make stripes, dots or checks with real CSS backgrounds.',primaryLabel:'pattern CSS',related:['tile','export']});
