import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Place a built-in icon exemplar on a bounded viewBox and optional keyline grid.',primaryLabel:'icon SVG',related:['stroke','optical']});
