import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Apply bounded manual scale and position corrections to a built-in icon.',primaryLabel:'adjusted icon SVG',related:['grid','stroke']});
