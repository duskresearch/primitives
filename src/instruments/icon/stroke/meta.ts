import {defineInstrument} from '@/lib/instrument';
export default defineInstrument({purpose:'Compare how an icon stroke scales against a fixed CSS-pixel stroke.',primaryLabel:'non-scaling icon SVG',related:['grid','optical']});
