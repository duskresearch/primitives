import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'Every size from one base and one ratio, with line heights, as CSS that can grow with the screen.',
  primaryLabel: 'CSS',
  related: ['units', 'measure', 'specimen'],
});
