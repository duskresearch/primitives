import { defineInstrument } from '@/lib/instrument';

export default defineInstrument({
  purpose: 'Your text in every Google Fonts family, sorted and filtered by how the faces measure, with pairings and embed code.',
  primaryLabel: 'embed code',
  related: ['fallback', 'scale', 'measure'],
});
