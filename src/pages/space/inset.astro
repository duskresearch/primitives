---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/space/inset/Tool.svelte';
import {parse,insetOwn,serializeWith} from '@/instruments/space/state';
export const prerender=false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=insetOwn(q);return {state,own,query:serializeWith(state,own)};};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="space" slug="inset" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
