---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/random/shuffle/Tool.svelte';
import {parse,shuffleOwn,serializeWith} from '@/instruments/random/state';
export const prerender=false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=shuffleOwn(q);return {state,own,query:serializeWith(state,own)};};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="random" slug="shuffle" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
