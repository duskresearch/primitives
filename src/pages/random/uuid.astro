---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/random/uuid/Tool.svelte';
import {parse,uuidOwn,serializeWith} from '@/instruments/random/state';
export const prerender=false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=uuidOwn(q);return {state,own,query:serializeWith(state,own)};};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="random" slug="uuid" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
