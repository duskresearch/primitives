---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/random/seed/Tool.svelte';
import {parse,seedOwn,serializeWith} from '@/instruments/random/state';
export const prerender=false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=seedOwn(q);return {state,own,query:serializeWith(state,own)};};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="random" slug="seed" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
