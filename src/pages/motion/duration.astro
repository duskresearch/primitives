---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/motion/duration/Tool.svelte';
import {parse,durationOwn,serializeWith} from '@/instruments/motion/state';
export const prerender=false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=durationOwn(q);return {state,own,query:serializeWith(state,own)};};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="motion" slug="duration" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
