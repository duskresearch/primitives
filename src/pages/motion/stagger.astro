---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/motion/stagger/Tool.svelte';
import {parse,staggerOwn,serializeWith} from '@/instruments/motion/state';
export const prerender=false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=staggerOwn(q);return {state,own,query:serializeWith(state,own)};};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="motion" slug="stagger" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
