---
import Instrument from '@/layouts/Instrument.astro';
import Tool from '@/instruments/shape/blob/Tool.svelte';
import { parse, blobOwn, serializeWith } from '@/instruments/shape/state';
export const prerender = false;
const read=(q:URLSearchParams)=>{const state=parse(q),own=blobOwn(q);return {state,own,query:serializeWith(state,own)}};
const {state,own,query}=read(Astro.url.searchParams);
const defaults=read(new URLSearchParams()).query;
---
<Instrument primitive="shape" slug="blob" {query} {defaults}><Tool client:load initial={state} {own}/></Instrument>
