// OG image geometry, shared by the generator (scripts/build-assets.mjs) and the page head.
// Laid out at the standard 1200×630 and rasterized at 2× so it stays sharp on dense screens.
export const OG = { width: 1200, height: 630, scale: 2 };
